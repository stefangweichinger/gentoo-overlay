#!/bin/bash

case "$1" in
	install)
		cd ${MY_INSTALLDIR}/themes
		find . -type d -mindepth 1 -maxdepth 1 \
			-exec mkdir '{}'/compiled ';' -and \
			-exec chown -R ${VHOST_SERVER_UID} '{}'/compiled ';'
		;;
	*)
		# Nothing to  clean
		;;
esac
